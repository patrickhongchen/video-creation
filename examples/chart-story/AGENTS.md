# Presentation Project

`presentation.json` is the source of truth. For a chart progression, preserve the chart ID, datum IDs, and shared frame identity. Slides use a 1080×1920 canvas and ordinary elements. Validate after editing.
