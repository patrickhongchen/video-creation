# Presentation Project

`presentation.json` is the source of truth. Assets belong in `assets/` and use project-relative paths. Slides use a fixed 1080×1920 canvas and contain ordinary elements. Keep one main idea per slide, preserve IDs for continuing concepts, and validate after editing.
