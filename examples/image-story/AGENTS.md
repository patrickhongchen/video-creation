# Presentation Project

`presentation.json` is the source of truth. Inspect and reuse `assets/`, keep paths project-relative, and use image frames large enough to read. Preserve IDs for continued concepts and validate after editing.
